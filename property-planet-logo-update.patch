diff --git a/public/assets/images/logo/new/property-planet-logo-dark.png b/public/assets/images/logo/new/property-planet-logo-dark.png
new file mode 100644
index 0000000..a5aedba
Binary files /dev/null and b/public/assets/images/logo/new/property-planet-logo-dark.png differ
diff --git a/public/assets/images/logo/new/property-planet-logo-light.png b/public/assets/images/logo/new/property-planet-logo-light.png
new file mode 100644
index 0000000..0e87bef
Binary files /dev/null and b/public/assets/images/logo/new/property-planet-logo-light.png differ
diff --git a/public/assets/scss/_brand-logo.scss b/public/assets/scss/_brand-logo.scss
index 769dac2..3c38b4c 100644
--- a/public/assets/scss/_brand-logo.scss
+++ b/public/assets/scss/_brand-logo.scss
@@ -1,160 +1,22 @@
 // -----------------------------------------------------------------------
-// Property Planet brand logo -- "Planet Orbit + Establish" entrance.
-// Used by src/components/common/BrandLogo.tsx.
-//
-// One-time entrance on mount (~900ms), then the logo is completely
-// static -- no idle loop, no continuous spin, no pulsing. Desktop hover
-// may replay the orbit sweep once. Artwork/geometry/colors are untouched;
-// only opacity/transform on group wrappers are animated.
+// Property Planet brand logo.
+// Used by src/components/common/BrandLogo.tsx and
+// src/components/common/AnimatedBrandLogo.tsx.
 //
-// Structure animated:
-//   .brand-logo__icon        outer icon group -- scale/opacity establish
-//   .brand-logo__orbit       ring + dot group -- subtle rotational settle
-//   .brand-logo__sweep       short arc highlight -- one pass along the ring
-//   .brand-logo__word        wordmark group -- fade + short translateX
+// Static raster artwork (no entrance animation). Two variants exist as
+// separate image files (light/navy wordmark, dark/white wordmark) --
+// see BrandLogo.tsx for how they're selected.
 // -----------------------------------------------------------------------
 
-@keyframes brandLogoIconIn {
-  0%   { opacity: 0; transform: scale(0.93); }
-  70%  { opacity: 1; transform: scale(0.965); }
-  100% { opacity: 1; transform: scale(1); }
-}
-
-@keyframes brandLogoOrbitSettle {
-  0%   { opacity: 0; transform: rotate(-6deg); }
-  100% { opacity: 1; transform: rotate(0deg); }
-}
-
-@keyframes brandLogoSweep {
-  0%   { opacity: 0; stroke-dashoffset: 100; }
-  15%  { opacity: 1; }
-  70%  { opacity: 1; }
-  100% { opacity: 0; stroke-dashoffset: 0; }
-}
-
-@keyframes brandLogoWordIn {
-  0%   { opacity: 0; transform: translateX(6px); }
-  100% { opacity: 1; transform: translateX(0); }
-}
-
 .brand-logo {
-  overflow: visible;
-  transform-box: fill-box;
-
-  // Static (footer / reduced-motion) state: everything already in its
-  // final, fully-settled position -- no animation-name at all.
-  .brand-logo__icon,
-  .brand-logo__orbit,
-  .brand-logo__word {
-    opacity: 1;
-    transform: none;
-  }
-  .brand-logo__sweep {
-    opacity: 0;
-  }
-
-  // Entrance-enabled state, applied only via the `.brand-logo--animate`
-  // modifier so the footer instance can opt out and render statically.
-  &.brand-logo--animate {
-    .brand-logo__icon {
-      transform-origin: 50px 50px;
-      opacity: 0;
-      animation: brandLogoIconIn 0.65s cubic-bezier(0.16, 1, 0.3, 1) forwards;
-    }
-
-    .brand-logo__orbit {
-      transform-origin: 50px 50px;
-      opacity: 0;
-      animation: brandLogoOrbitSettle 0.5s cubic-bezier(0.22, 1, 0.36, 1) forwards;
-      animation-delay: 0.1s;
-    }
-
-    .brand-logo__sweep {
-      opacity: 0;
-      stroke-dasharray: 40 100;
-      stroke-dashoffset: 100;
-      animation: brandLogoSweep 0.55s ease-out forwards;
-      animation-delay: 0.25s;
-    }
-
-    .brand-logo__word {
-      opacity: 0;
-      animation: brandLogoWordIn 0.4s ease-out forwards;
-      animation-delay: 0.5s;
-    }
-
-    // Desktop-only: replay the orbit sweep once on hover. Never wired
-    // up for touch, and never a looping/continuous hover state.
-    @media (hover: hover) and (pointer: fine) {
-      &:hover .brand-logo__sweep {
-        animation-name: brandLogoSweep;
-        animation-duration: 0.55s;
-        animation-timing-function: ease-out;
-        animation-delay: 0s;
-        animation-fill-mode: forwards;
-      }
-    }
-  }
-}
-
-@media (prefers-reduced-motion: reduce) {
-  .brand-logo.brand-logo--animate {
-    .brand-logo__icon,
-    .brand-logo__orbit,
-    .brand-logo__word {
-      animation: none !important;
-      opacity: 1 !important;
-      transform: none !important;
-    }
-    .brand-logo__sweep {
-      animation: none !important;
-      opacity: 0 !important;
-    }
-  }
+  display: inline-block;
+  height: auto;
 }
 
-// -----------------------------------------------------------------------
-// Header logo: video-based intro clip (public/assets/video/brand-logo-anim.*)
-// layered over the static BrandLogo SVG. Used by
-// src/components/common/AnimatedBrandLogo.tsx.
-//
-// The static SVG always occupies the box (no layout shift, single
-// accessible name); the video sits on top while playing and is faded
-// out once it reaches its final frame, revealing the static SVG as the
-// permanent resting state. prefers-reduced-motion never renders a
-// <video> at all, so this box always just shows the static SVG then.
-// -----------------------------------------------------------------------
 .brand-logo-video-wrap {
   position: relative;
   display: inline-block;
-  width: 380px;
-  height: 95px;
+  width: 220px;
   max-width: 100%;
   overflow: hidden;
 }
-
-.brand-logo-video-fallback,
-.brand-logo-video {
-  position: absolute;
-  inset: 0;
-  width: 100%;
-  height: 100%;
-  opacity: 1;
-  transition: opacity 0.35s ease;
-}
-
-.brand-logo-video-fallback.is-hidden {
-  opacity: 0;
-}
-
-.brand-logo-video {
-  object-fit: contain;
-  object-position: left center;
-  border: 0;
-  outline: none;
-  pointer-events: none;
-
-  &.is-hidden {
-    opacity: 0;
-  }
-}
\ No newline at end of file
diff --git a/src/components/common/AnimatedBrandLogo.tsx b/src/components/common/AnimatedBrandLogo.tsx
index 3a5a86b..2df8a2e 100644
--- a/src/components/common/AnimatedBrandLogo.tsx
+++ b/src/components/common/AnimatedBrandLogo.tsx
@@ -1,57 +1,17 @@
 // src/components/common/AnimatedBrandLogo.tsx
 //
-// Header logo: plays the Property Planet intro clip on a continuous,
-// silent loop -- it never stops and never needs a click. Falls back to
-// the static BrandLogo SVG only while the video hasn't started playing
-// yet (or can't play at all, e.g. prefers-reduced-motion, or the file
-// fails to load), so there's never a blank gap in the header.
-//
-// Always renders BrandLogo underneath so there is a) no layout shift and
-// b) a single accessible name ("Property Planet") whether or not the
-// video is showing -- the video itself is aria-hidden.
-
-"use client"
+// Header logo. Previously layered a choreographed intro video over the
+// old hand-drawn SVG mark. That video was built frame-by-frame for the
+// old logo's specific geometry, so it has no valid equivalent for the
+// new Property Planet artwork -- rather than play a mismatched
+// animation over new art, this renders the static logo directly.
 
-import { useEffect, useState } from "react"
-import BrandLogo from "./BrandLogo"
+import BrandLogo from "./BrandLogo";
 
 const AnimatedBrandLogo = () => {
-   const [canAnimate, setCanAnimate] = useState(false);
-   const [videoPlaying, setVideoPlaying] = useState(false);
-   const [videoFailed, setVideoFailed] = useState(false);
-
-   useEffect(() => {
-      const mq = window.matchMedia("(prefers-reduced-motion: reduce)");
-      const applyPreference = () => setCanAnimate(!mq.matches);
-      applyPreference();
-      mq.addEventListener("change", applyPreference);
-      return () => mq.removeEventListener("change", applyPreference);
-   }, []);
-
-   const showVideo = canAnimate && !videoFailed;
-   const showFallback = !showVideo || !videoPlaying;
-
    return (
       <span className="brand-logo-video-wrap">
-         <BrandLogo
-            animate={false}
-            className={`brand-logo-video-fallback${showFallback ? "" : " is-hidden"}`}
-         />
-         {showVideo && (
-            <video
-               className={`brand-logo-video${videoPlaying ? "" : " is-hidden"}`}
-               autoPlay
-               loop
-               muted
-               playsInline
-               preload="auto"
-               aria-hidden="true"
-               onPlaying={() => setVideoPlaying(true)}
-               onError={() => setVideoFailed(true)}
-            >
-               <source src="/assets/video/brand-logo-anim.mp4" type="video/mp4" />
-            </video>
-         )}
+         <BrandLogo variant="light" />
       </span>
    );
 };
diff --git a/src/components/common/BrandLogo.tsx b/src/components/common/BrandLogo.tsx
index ec2348b..bfadab4 100644
--- a/src/components/common/BrandLogo.tsx
+++ b/src/components/common/BrandLogo.tsx
@@ -1,89 +1,41 @@
 // src/components/common/BrandLogo.tsx
 //
-// Reusable Property Planet logo. Inline SVG so individual groups (icon,
-// orbit, wordmark) can be targeted by the "Planet Orbit + Establish"
-// entrance animation in public/assets/scss/_brand-logo.scss.
+// Property Planet logo. Static raster artwork (replaces the previous
+// hand-drawn inline SVG). Two pre-rendered variants are shipped:
+//   variant="light" -> @/assets/images/logo/new/property-planet-logo-light.png
+//                      (navy wordmark) for light-background placements
+//                      (header, light footer).
+//   variant="dark"  -> @/assets/images/logo/new/property-planet-logo-dark.png
+//                      (white wordmark, same icon artwork) for dark-
+//                      background placements (dark footer).
 //
-// Artwork is copied verbatim from the existing static logo files -- same
-// shapes, positions, and brand colors, no redesign or geometry changes.
-// When animation finishes (or is skipped), the rendered result is
-// pixel-identical to the static SVGs they replace:
-//   variant="light" -> public/assets/images/logo/logo_02.svg (header default)
-//   variant="dark"  -> public/assets/images/logo/logo_03.svg (dark-bg footer)
-//
-// Usage:
-//   <BrandLogo />                          header: entrance once on mount
-//   <BrandLogo variant="dark" animate={false} /> footer: static, dark-bg colors
+// The `animate` prop is kept for backwards compatibility with existing
+// call sites but is now a no-op: this artwork has no entrance animation.
+// See AnimatedBrandLogo.tsx for the header's own static-image handling.
+
+import Image from "next/image";
+import lightLogo from "@/assets/images/logo/new/property-planet-logo-light.png";
+import darkLogo from "@/assets/images/logo/new/property-planet-logo-dark.png";
 
 interface BrandLogoProps {
-   /** Play the entrance animation on mount. Defaults to true. Pass false
-    *  for static placements (e.g. the footer) that should never animate. */
+   /** No longer used -- kept so existing call sites don't need to change. */
    animate?: boolean;
-   /** Color scheme matching the background it sits on. "light" mirrors
-    *  logo_02.svg (light bg). "dark" mirrors logo_03.svg (dark-bg footer). */
+   /** "light" for light backgrounds (navy wordmark), "dark" for dark
+    *  backgrounds (white wordmark). */
    variant?: "light" | "dark";
    className?: string;
 }
 
-const PALETTES = {
-   light: { ring: "#157567", ringOpacity: undefined as number | undefined, accent: "#FF6725", fill: "#e7f4f1", houseBody: "#157567", houseWindow: "#e7f4f1", houseWindowStroke: undefined as string | undefined, wordPrimary: "#157567" },
-   dark: { ring: "#ffffff", ringOpacity: 0.9, accent: "#FF8B5C", fill: "none", houseBody: "#ffffff", houseWindow: "none", houseWindowStroke: "#FF8B5C", wordPrimary: "#ffffff" },
-};
-
-const BrandLogo = ({ animate = true, variant = "light", className = "" }: BrandLogoProps) => {
-   const p = PALETTES[variant];
+const BrandLogo = ({ variant = "light", className = "" }: BrandLogoProps) => {
+   const src = variant === "dark" ? darkLogo : lightLogo;
    return (
-      <svg
-         width="380"
-         height="95"
-         viewBox="0 0 440 110"
-         xmlns="http://www.w3.org/2000/svg"
-         className={`brand-logo ${animate ? "brand-logo--animate" : ""} ${className}`}
-         role="img"
-         aria-label="Property Planet"
-      >
-         <g transform="translate(10,5)">
-            <g className="brand-logo__icon">
-               <circle cx="50" cy="50" r="44" fill={p.fill} />
-               <circle cx="50" cy="50" r="44" fill="none" stroke={p.ring} strokeWidth={5} opacity={p.ringOpacity} />
-            </g>
-
-            <g className="brand-logo__orbit">
-               <ellipse
-                  cx="50" cy="50" rx="52" ry="18"
-                  fill="none" stroke={p.accent} strokeWidth={5.5}
-                  transform="rotate(-18 50 50)"
-               />
-               <circle cx="96" cy="41" r="5.5" fill={p.accent} transform="rotate(-18 50 50)" />
-            </g>
-
-            {/* Restrained one-pass sweep along the orbit ring -- a short
-                arc segment that fades in, travels, and fades out once.
-                Not a looping/continuous effect. */}
-            <ellipse
-               cx="50" cy="50" rx="52" ry="18"
-               fill="none" stroke={p.accent} strokeWidth={5.5}
-               strokeLinecap="round"
-               transform="rotate(-18 50 50)"
-               pathLength={100}
-               className="brand-logo__sweep"
-            />
-
-            <g className="brand-logo__icon">
-               <polygon points="50,22 80,50 20,50" fill={p.accent} />
-               <rect x="28" y="50" width="44" height="34" fill={p.houseBody} />
-               <rect x="44" y="62" width="14" height="22" fill={p.houseWindow} stroke={p.houseWindowStroke} strokeWidth={p.houseWindowStroke ? 2 : undefined} />
-            </g>
-         </g>
-         <g className="brand-logo__word" fontFamily="Arial, Helvetica, sans-serif" fontWeight={800}>
-            <text x="125" y="48" fontSize="34">
-               <tspan fill={p.wordPrimary}>PROPER</tspan><tspan fill={p.accent}>ty</tspan>
-            </text>
-            <text x="125" y="86" fontSize="34">
-               <tspan fill={p.wordPrimary}>PLAN</tspan><tspan fill={p.accent}>et</tspan>
-            </text>
-         </g>
-      </svg>
+      <Image
+         src={src}
+         alt="Property Planet"
+         className={`brand-logo ${className}`}
+         style={{ height: "auto", width: "100%", maxWidth: 220 }}
+         priority
+      />
    );
 };
 
diff --git a/src/layouts/headers/Menu/Offcanvas.tsx b/src/layouts/headers/Menu/Offcanvas.tsx
index 0524540..d2b1399 100644
--- a/src/layouts/headers/Menu/Offcanvas.tsx
+++ b/src/layouts/headers/Menu/Offcanvas.tsx
@@ -1,9 +1,8 @@
 "use client"
-import Image from "next/image"
 import Link from "next/link"
 import { CONTACT_EMAIL, CONTACT_PHONE_DISPLAY, CONTACT_PHONE_HREF, SOCIAL_LINKS } from "@/lib/site/contact"
 
-import offcanvasLogo from "@/assets/images/logo/logo_02.svg"
+import BrandLogo from "@/components/common/BrandLogo"
 
 // Phase 20: this panel is opened by the "Menu" button in the live header,
 // so everything in it was public. It previously showed three fabricated
@@ -38,7 +37,7 @@ const Offcanvas = ({ offCanvas, setOffCanvas }: any) => {
             <div className="offcanvas-header">
                <div className="logo order-lg-0">
                   <Link href="/" className="d-flex align-items-center">
-                     <Image src={offcanvasLogo} alt="Property Planet" />
+                     <BrandLogo variant="light" />
                   </Link>
                </div>
                <button onClick={() => setOffCanvas(false)} type="button" className="btn-close" data-bs-dismiss="offcanvas" aria-label="Close menu"></button>
